/** Delay (ms) between a correct word being scored and the next word appearing. */
export const NEXT_WORD_DELAY_MS = 800;

/** Duration (ms) of the bonus-word highlight animation. */
export const BONUS_ANIMATION_MS = 450;

/** Delay (ms) before transitioning to the game-over screen after the final word reveal. */
export const GAME_OVER_REVEAL_MS = 1500;

/** Duration (ms) of the score delta pop-up animation. */
export const SCORE_POPUP_DURATION_MS = 900;

/** Duration (ms) for which the "copied" confirmation is shown after copying the room code. */
export const COPY_FEEDBACK_MS = 2000;

/** Duration (ms) of the tier-transition message in competition mode. */
export const TIER_TRANSITION_MS = 1500;
